# 2026-06-12 - reloj

## Cambios realizados por Codex
- Se agregó `scripts/reloj.sh` para instalar y configurar Chrony con zona horaria `America/Panama`.
- Se genera una configuración limpia de `/etc/chrony/chrony.conf` con `rtcsync` y `makestep`.
- Se crea y habilita `hwclock.service` para guardar el reloj del sistema en el reloj de hardware al apagado.
- Se ejecuta una sincronización inicial con `chronyc makestep` y `hwclock --systohc`.

## Ajuste posterior
- Se corrigió `hwclock.service` para que permanezca `inactive (dead)` en operación normal.
- Se eliminó `RemainAfterExit=yes`, `ExecStart=/bin/true` y el vínculo a `multi-user.target` que dejaban el servicio `active (exited)`.
- El servicio ahora se habilita contra `shutdown.target` y ejecuta `hwclock --systohc` al iniciar el apagado.
