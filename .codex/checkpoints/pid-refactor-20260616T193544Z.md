# PID refactor checkpoint

- Corregido `func/pid.py` para no depender de imports inexistentes.
- Renombrado `current_pump_percent` a `initial_output` en el controlador PID genérico.
- Integrado `PIDController` persistente desde `func/control.py` mediante pool compartido.
- Eliminada lógica PID duplicada para válvula en los típicos con válvula.
- Reemplazado el PID local de temperatura/humedad/heater por llamadas al PID genérico.
